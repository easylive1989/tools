name: Monthly Ledger Analysis

on:
  schedule:
    # 每月1號早上8:00 (UTC)，調整為台灣時區是晚上8:00
    # 改為午夜0:00 UTC，對應台灣時間早上8:00
    - cron: '0 0 1 * *'
  workflow_dispatch: # 允許手動觸發

jobs:
  ledger-analysis:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        # 如果沒有 requirements.txt，安裝必要的套件
        pip install requests
        
    - name: Run ledger analysis
      env:
        # 將 Notion API secret 設為環境變數
        NOTION_SECRET: ${{ secrets.NOTION_SECRET }}
      run: |
        python ledger_analysis/ledger_analysis.py